pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
    plugins {
        id 'java'
        id 'maven-publish'
    }
}

rootProject.name = "VideoAnalyticsLogistics"